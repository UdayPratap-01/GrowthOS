{
  "name": "growthos-ai",
  "private": true,
  "scripts": {
    "dev:api": "cd apps/api && .venv/bin/uvicorn app.main:app --reload --port 8000",
    "dev:web": "cd apps/web && npm run dev",
    "seed": "cd apps/api && PYTHONPATH=. .venv/bin/python -m app.demo.seed",
    "test:api": "cd apps/api && PYTHONPATH=. .venv/bin/pytest -q"
  }
}
